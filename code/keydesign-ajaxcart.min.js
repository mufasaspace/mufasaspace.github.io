(function (a) {
    "use strict";

    function b() {
        window.wc_add_to_cart_params != void 0 && a.post({
            url: wc_add_to_cart_params.ajax_url,
            dataType: 'JSON',
            data: {
                action: 'woomenucart_ajax'
            },
            success: function (b, c, d) {
                a('.keydesign-cart-dropdown').html(b.cart), b != '' && (a('.keydesign-cart .badge, .mobile-shopping-cart .badge').length ? b.articles > 0 ? (a('.keydesign-cart .badge, .mobile-shopping-cart .badge').html(b.articles), a('.keydesign-cart .badge, .mobile-shopping-cart .badge').show()) : a('.keydesign-cart .badge, .mobile-shopping-cart .badge').hide() : a('.keydesign-cart .cart-icon-container').append('<span class="badge">' + b.articles + '</span>'))
            }
        })
    }
    a(document).ready(function () {
        a('body').bind("added_to_cart", b), a('body').bind("wc_fragments_refreshed", b)
    })
})(jQuery)
